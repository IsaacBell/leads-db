'use server'

export const searchLeads = (searchOpts: any) => {};
export const getSearchPrefs = () => { };
export const enrichLead = (leadId: string) => { };
export const createCollection = (leads: string[]) => { };
